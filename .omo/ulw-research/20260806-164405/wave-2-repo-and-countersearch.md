# Wave 2 digest

- MCP Python v1 is maintenance-only; Agent Lab already pins `<2` and currently runs 1.27.2, preventing an accidental v2 cutover.
- ACP protocol v1 shipped in June 2026 and repo changelog is at 1.6.0, but v2 and remote transports remain in motion.
- AgentGUI is a strong interaction reference with limited runtime coverage; CWC is explicitly a demo; Attractor is an NLSpec; Open SWE has strong sandbox/trigger patterns but weaker prompt-driven validation; A2UI v1 is still candidate.
- Recent eval work shows benchmark task quality, harness configuration, infrastructure budget, dialogue quality, and repository exploration must be evaluated separately.

## EXPAND

none — remaining questions are implementation spikes or live dogfood experiments rather than unsearched evidence leads.
