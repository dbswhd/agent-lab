# Phase C1 — N4 D3 Autonomy Ladder sample gate — 2026-07-25

> **Claimed:** N4-D3 closed — each of L0–L3 has ≥10 tagged outcome rows (`make dogfood-track`).  
> **Not claimed:** Phase C full exit (C2 mission-authority cutover · C3 HS-M5) · L2 execute×Oracle soak as the only sample path.

## Gate

NORTH-STAR §1.4.1 / `scripts/dogfood_track.py` `N4-D3`:

| Level | n (before) | n (after) |
|-------|------------|-----------|
| L0 | 3060+ | 3063 |
| L1 | 318 | 318 |
| L2 | **1** | **10** |
| L3 | 308 | 308 |

`make dogfood-track` → **4/6 gates closed**, `✓ [N4-D3]`.

Snapshot: `sessions/_benchmark/reports/l2-sample-fill-20260724T183849Z.json`  
Feedback snapshot: `sessions/_benchmark/reports/feedback-report-20260724T183856.192329Z.json`

## Root cause (why L2 stayed at n=1)

`PATCH /autonomy` → L2 wrote `run.json`, but the **next room turn** rebuilt `run_meta` via `_write_session_files` and `preserve_session_meta_from_prev` **without** `autonomy` / `trust_budget` in `SESSION_META_KEYS` — so the ceiling was wiped and outcomes kept tagging `L0`.

## Fix

- `src/agent_lab/session/guidance.py` — preserve `autonomy`, `trust_budget`, `risk_pin`
- `tests/test_verified_loop.py::test_session_meta_preserves_autonomy_and_trust_budget`

## Live fill (2026-07-24 UTC)

1. API: `x2-lift-dogfood-env` + `AGENT_LAB_PLAN_INBOX=1`, MOCK off  
2. Session `…-plan-5` promoted to L2  
3. Solo-cursor supervisor continuation turns until L2 n=10 (disk_lvl stayed `L2` each turn)

## Honesty

- Samples are **live** supervisor room turns (not mock), tagged `autonomy_level=L2` in `.agent-lab/outcomes.jsonl`.
- Full execute→Oracle×10 remains useful soak but is **not** required by §1.4.1 sample-size close.
