# Phase C3 — HS-M5 harness self-patch — 2026-07-25

> **Claimed:** One live Human-approved harness_patch merge for an addressable weakness pattern.  
> **Not claimed:** HS6 DGM-lite · automatic proposer without Human · Tier B eval-surface edits.

## Gate

HSIL HS-M5 / `make dogfood-track` **HS-M5**: addressable≥1 **and** harness_patch merge ≥1.

| Field | Value |
|-------|-------|
| pattern | `fp:weak_taste:deep` (recurrence=3) |
| candidate | `pc-2026-07-24-fc0b8932` |
| axis / tier | prompts / A |
| HS4 regress | **pass** (assertions + held-out + smoke) |
| merge commit | `755c38d96d6e8fd1a76d8190d06d76d9db5ba8c3` |
| harness_rev | `manifest@sha:755c38d96d6e8fd1a76d8190d06d76d9db5ba8c3` |

## Patch (summary)

Claude room prompt: CHALLENGE once with concrete ref + satisfaction condition; ENDORSE after peer addresses; BLOCK only for documented invariant/moat violations.

## Commands

```bash
python scripts/propose_harness.py --mode propose --pattern-id fp:weak_taste:deep ...
AGENT_LAB_REGRESSION_GATE=1 python scripts/regress_harness.py --candidate-id pc-… --diff-path …
AGENT_LAB_HARNESS_INBOX=1 python scripts/merge_harness.py --mode merge --candidate-id pc-…
make dogfood-track-hs-m5-merge CANDIDATE_ID=pc-… NOTES='…'
```

## Enabling work for HS4

Held-out green required baseline refresh + worktree-safe mypy (`--follow-imports=silent`) — see companion commits on this branch before the harness_patch commit.

## Honesty

Offline `merge_harness --mode merge` **is** Human approval per HSIL (“명령 실행이 곧 human 승인”). Inbox UI path remains available via `--mode propose` + session folder.
